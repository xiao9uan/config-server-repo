spring:
  application:
    name: eureka-server-master
    
eureka:
  instance:
    appname: eureka-server-master
    hostname: localhost
    
  client:
    register-with-eureka: true
    fetch-registry: true
    
